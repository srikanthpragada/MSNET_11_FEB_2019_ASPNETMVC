<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books - @ViewBag.Title </title>
    @Styles.Render("~/Content/css")
</head>
<body>
   <div class="banner">Books</div>
   <div class="menu">
       [@Html.ActionLink("Home", "Home", "Books")]
       &nbsp;
       [@Html.ActionLink("Add Book", "Add", "Books")]
       &nbsp;
       [@Html.ActionLink("List Books", "List", "Books")]
       &nbsp;
       [@Html.ActionLink("Serach Books", "Search", "Books")]
   </div>

   @RenderBody()

   @Scripts.Render("~/bundles/jquery")
   @RenderSection("scripts", required: false)
</body>
</html>
